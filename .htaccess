<IfModule mod_rewrite.c>
    RewriteEngine On

    # Only rewrite on the first pass (prevents infinite loop)
    RewriteCond %{ENV:REDIRECT_STATUS} ^$
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
